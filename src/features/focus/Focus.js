import React from "react";
import { Text, View } from "react-native";

export const Focus = () => {
  return (
    <View>
      <Text>What you working on?</Text>
    </View>
  );
};


const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#454545'
    }
})
